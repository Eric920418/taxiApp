GoGoCha v1.4.5 確認目的地 bug v2 修法：

🐛 v1.4.4 修法沒生效 — 按完還是一直跳出來
   - 原因：先呼叫 API 再更新 set，如果 API 拋（網路斷/auth/404）
     try 流程斷掉，set 永遠加不進去 → WebSocket 重推時按鈕又跳
   - 修正：樂觀更新模式
     1. 先同步更新 UI + set（不可被 exception 阻斷）
     2. API 背景送，失敗只 warn 不阻 UI
   - 後端目前無對應持久化 endpoint，純前端兜底

—— GoGoCha 花蓮計程車
